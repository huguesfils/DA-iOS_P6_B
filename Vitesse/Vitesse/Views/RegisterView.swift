import SwiftUI

struct RegisterView: View {
    var body: some View {
        VStack {
            Text("Register")
                .font(.largeTitle)
                .padding()

            Spacer()
        }
        .navigationTitle("Register")
    }
}

#Preview {
    RegisterView()
}
